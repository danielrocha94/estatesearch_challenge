<div class="relative max-h-screen w-full flex flex-col items-center">
  <div class="h-full overflow-y-auto mb-26">
    <%= turbo_frame_tag :new_post, src: new_form_posts_path, class: "flex justify-center self-stretch" %>
    <%= turbo_frame_tag :posts, src: posts_path(user_id: current_user.id), class: "flex flex-col items-center justify-center self-stretch" %>
  </div>
</div>
